{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chrome-capability",
  "description": "Permissions for the trusted local browser chrome only.",
  "webviews": ["chrome"],
  "permissions": [
    "core:default",
    "dialog:allow-save"
  ]
}
